{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe{% endblock %}

{% block content %}
    <h1>Modifier mon mot de passe</h1><br><br>

    - <a href="{{ path('account') }}">Retour à mon compte</a> <br>
    <hr>
    {% if notification %}
        <div class="container alert alert-info text-center">
            {{ notification }}
        </div>
    {% endif %}
    {{ form(form) }}
    <hr>
{% endblock %}
